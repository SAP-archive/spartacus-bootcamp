import { LayoutConfig } from '@spartacus/storefront';

export const layoutConfig: LayoutConfig = {
  layoutSlots: {
    ContentPage1Template: {
      slots: ['StaticSlot', 'Section2A', 'Section2B'],
    },
  },
};
